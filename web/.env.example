# API FastAPI pública (sin barra final). En local, déjala vacía: Vite hace proxy de /api.
# En Vercel, configura en Project → Settings → Environment Variables.
# Ejemplo: https://mi-api.onrender.com
VITE_API_BASE_URL=
